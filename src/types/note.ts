//Загальні типи, які використовуються в кількох компонентах,
//  винесені в окремий файл (src/types/note.ts).
//  Типи та interface, які стосуються лише одного компонента,
// оголошені безпосередньо у файлі цього компонента.

//Інтерфейс для пропсів компонента називається за схемою:
// Ім’яКомпонентаProps (наприклад, UserCardProps).
